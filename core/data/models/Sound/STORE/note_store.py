from core.data.models.Sound.Note import Note

DEFAULT_FREQUENCY=44100

A=Note(
    frequency=DEFAULT_FREQUENCY,
    value="A")
ASharp=Note(
    frequency=DEFAULT_FREQUENCY,
    value="ASharp")

B=Note(
    frequency=DEFAULT_FREQUENCY,
    value="B")

BSharp=Note(
    frequency=DEFAULT_FREQUENCY,
    value="BSharp")